module main

import "./tasks.pkl"

shebang = "#!/usr/bin/env ansible-playbook\n"
dashes = "---\n"
r = new YamlRenderer{}

output {
    files {

        ["tasks/main.yml"] {
            text = "\(dashes)\(r.renderDocument(tasks.main))"
        }

    }
}

